// StatLang, пример 3 — минимальный, для отладки грамматики.
// Одна подпрограмма, ввод, цикл, вывод.

int square(int x) {
    return x * x;
}

int n = read();
int i = 1;
while (i <= n) {
    write("square(" + i + ") = " + square(i));
    i = i + 1;
}
