⍝ Не запускалось: нужен интерпретатор APL (например, Dyalog APL 19.0).
⍝ --8<-- [start:main]
∇ R←OUTER;X          ⍝ X — локальное имя OUTER
  X←10
  R←INNER            ⍝ INNER не объявляет X
∇
∇ R←INNER
  R←X+1              ⍝ какое X? ищется по цепочке вызовов
∇
X←1
OUTER                ⍝ 11: INNER видит локальную X вызывающей функции
INNER                ⍝ 2: глобальная X
⍝ --8<-- [end:main]
