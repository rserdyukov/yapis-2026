\ --8<-- [start:main]
3 5 < . CR           \ истина — ячейка, в которой все биты 1, то есть -1
3 5 < 10 * . CR      \ флаг — просто число: его можно умножить
1 +                  \ второго слагаемого на стеке нет
\ --8<-- [end:main]
