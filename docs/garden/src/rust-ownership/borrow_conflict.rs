// Ожидается ошибка компиляции E0502: разделяемое и исключительное
// заимствование одного значения одновременно.
fn main() {
    let mut v = vec![1, 2, 3];
    let first = &v[0]; // разделяемое заимствование v
    v.push(4); // push требует &mut v, а first ещё жив
    println!("{first}");
}
