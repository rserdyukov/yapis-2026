// Ожидается ошибка компиляции E0597: ссылка переживает значение.
fn main() {
    let r: &String;
    {
        let s = String::from("временная");
        r = &s; // заимствуем s ...
    } // ... но s уничтожается здесь
    println!("{r}"); // висячая ссылка — компилятор не пропустит
}
