# Зависимости сборки сайта курса (GitHub Pages).
# Версии зафиксированы: сборка должна быть воспроизводимой.
mkdocs==1.6.1
mkdocs-material==9.5.49
# Читает данные каталога языков в tools/build-catalog.py. MkDocs тянет его
# транзитивно, но генератор запускается и без MkDocs (тесты), поэтому явно.
pyyaml==6.0.3
